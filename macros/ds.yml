---
version: 2

macros:
  - name: ds
    description: |
      ## Overview
      It enables us to get a date string which derives from the environment variable `EXECUTION_DATE`.
      The output should follow `%Y-%m-%d`.
      It is inspired by Apache Airflow's `{% raw %}{{ ds }}{% endraw %}`.

      ## Usage
      {% raw %}`{{ dbt_airflow_macros.ds() }}`{% endraw %}}
      {% raw %}`{{ dbt_airflow_macros.ds(timezone="Asia/Tokyo") }}`{% endraw %}}
